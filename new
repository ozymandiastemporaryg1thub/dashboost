while task.wait() do
	if game.Players.LocalPlayer.Character ~= nil then
		local char = game.Players.LocalPlayer.Character
		if char:FindFirstChild("HumanoidRootPart") then
			local humroot = char:FindFirstChild("HumanoidRootPart")
			if humroot:FindFirstChild("DashVelocity") then
				local velo = humroot:FindFirstChild("DashVelocity")
				velo.MaxForce = Vector3.new(999999999999,0,999999999999)*252
				print(velo.Velocity)
				print(velo.MaxForce)
				print(velo.P)
			elseif humroot:FindFirstChild("FrontDashVelocity") then
				local velo = humroot:FindFirstChild("FrontDashVelocity")
				velo.MaxForce = Vector3.new(999999999999,0,999999999999)*252
				print(velo.Velocity)
				print(velo.MaxForce)
				print(velo.P)
			end
			if char:FindFirstChildOfClass("Humanoid") then
				local hum = char:FindFirstChildOfClass("Humanoid")
				if hum.WalkSpeed == 16 then
					hum.WalkSpeed = 18.75
				elseif hum.WalkSpeed == 0 then
					if humroot:FindFirstChild("KnockbackVelocity") then
						if humroot:FindFirstChild("FrontDashVelocity") then
							--no
						else
							if humroot:FindFirstChild("MartialArtistFrontDashSwing") then
								--no
							else
								if hum:GetState() ~= Enum.HumanoidStateType.Physics then
									local velo = humroot:FindFirstChild("KnockbackVelocity")
									velo.Velocity = velo.Velocity*1.045
									print(velo.Velocity)
									print(velo.MaxForce)
									print(velo.P)
								end
							end
						end
					elseif humroot:FindFirstChild("RollVelocity") then
						if hum.FloorMaterial ~= Enum.Material.Air then
							local velo = humroot:FindFirstChild("RollVelocity")
							velo.Velocity = velo.Velocity*1.075
							print(velo.Velocity)
							print(velo.MaxForce)
							print(velo.P)
						end
					end
				end
			end
		end
	end
end
